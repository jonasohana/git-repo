const laugh = function displayLaugh(num) {
    const msg = "";
    for(var i=1;i<=num;i++){
        msg+="ha";
    }    
    msg+="!";
    return(msg); 
    }

    laugh; // display function
    laugh(2) // pass argument